[package]
name = "db"
version = "0.1.0"
edition = "2024"

[dependencies]
log = "0.4"
sea-orm = { version = "2.0", features = [
    "sqlx-postgres",
    "runtime-tokio-rustls",
    "macros",
] }
shared = { path = "../shared" }
